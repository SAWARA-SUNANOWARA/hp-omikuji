# Hotline セットアップ手順

同じバンドを推している友達と2人だけでやり取りできる、通知なしの簡易連絡ページです。
既存のおみくじカウンター機能と同じ仕組み(GitHub Pages + Google Apps Script + Googleスプレッドシート)で動きます。無料で、あなた自身のGoogleアカウントの中だけでデータを持てます。

## 特徴

- LINEのように本名が出ない。なぞった文字で自動的に人物名が決まる(下記参照)。
- プッシュ通知は一切送信しない。自分がページを開いたときだけ、その時点までのメッセージを取得する。
- Discord/SlackやXのDMとは完全に別の独立したページ。
- URLを開くと本物のおみくじ画面(`fortune.html`)がそのまま立ち上がり、普通に遊べる。画面を1.5秒以内に「右→左下→右」となぞる(Zの字)とサワラ、「上→右下→上」となぞる(Nの字)とヒイラギとして、隠しチャット画面`hotline-chat.html`に移動する。
- チャットの認証は**タブを開いている間だけ**有効。タブを閉じると自動的にログアウトされ、次にアクセスしたときは再度Z/Nジェスチャーが必要になる。
- 実際の読み書きはサーバー側(Apps Script)で合言葉を照合するので、なぞり方やURLを知らない人はメッセージを見られない。

## 画面構成・認証の仕組み

- `fortune.html` … 表向きのダミー画面。中身は本物のおみくじ(`index.html`)と同一で、通常のタップ操作にはジェスチャーの監視は一切干渉しない。
- `hotline-chat.html` … 実際のチャット画面。`fortune.html`からZ/Nジェスチャーで遷移してきたときだけ使える。
  - 合言葉(`?key=...`)となぞった文字(`?as=sawara`または`?as=hiiragi`)をURL経由で受け取り、`sessionStorage`(タブを閉じると自動で消える領域)に保存する。保存後はURLから合言葉を消す。
  - 直接このURLを(セッションが無い状態で)開いても、何も表示せず`fortune.html`に戻されるだけ。パスワード入力欄やエラーメッセージは一切出さない。
  - なぞる文字と人物名の対応は固定: **Z → サワラ / N → ヒイラギ**。ニックネームの入力操作はもう無い。

## ここまで自動で完了していること

- 専用のGoogleスプレッドシートを作成済み:
  https://docs.google.com/spreadsheets/d/11qc3xptYhTIghD-wWEj8W2X5eJ0BzRbr4IvvZBuQymM/edit
- 合言葉をランダム生成済み(チャットの返信を参照)。
  **このリポジトリは公開(public)なので、合言葉の実際の値はコード内に書かず、Apps Scriptエディタ上でのみ設定してください。**
- Apps Scriptのデプロイ・`hotline-data.js`へのURL設定は完了済み。

## 残りの手順(コードを変更した場合のみ)

Apps Script側のコード(`apps-script/hotline-backend.gs`)を書き換えたときは、以下の手順で再デプロイが必要です。

### 1. Apps Scriptを開く

1. 上記スプレッドシートを開く。
2. メニューから「拡張機能」→「Apps Script」を開く。
3. コードを更新する。

### 2. 再デプロイ

「デプロイ」→「デプロイを管理」→ 既存のデプロイを編集 → バージョンを新しくする、で更新できます(URLは変わりません)。

## 使い方(友達への共有)

1. `https://<ユーザー名>.github.io/hp-omikuji/fortune.html?key=合言葉` というURLを、LINEなど**別の経路で一度だけ**こっそり友達に伝える。
2. 開くと普通のおみくじ画面が出る。相手とやり取りしたい時だけ、画面を1.5秒以内になぞる。
   - 自分がサワラなら「右→左下→右」(Z)
   - 自分がヒイラギなら「上→右下→上」(N)
3. なぞった文字に応じた人物としてチャット画面が開く。名前や合言葉を入力する操作は無い。
4. タブを閉じると自動でログアウトされる。次に使うときは、また`fortune.html?key=合言葉`を開いてジェスチャーをなぞる。

## 注意点

- `fortune.html` / `hotline-chat.html` はどちらもトップページ(`index.html`)からはリンクしていません。URLを知っている人しかたどり着けません。
- ただしリポジトリが公開(public)の場合、`fortune.html`のジェスチャー判定コードや`apps-script/hotline-backend.gs`のファイル自体は誰でも読めます。つまり「なぞり方」自体やZ/Nと人物名の対応は隠しきれません。実際のセキュリティは、Apps Script側で照合する**合言葉(SECRET)**が担っています。**SECRETは必ず自分で書き換えたものを使い、貼り付けたままの値を使わないでください。**
- セッションは`sessionStorage`を使っているため、原則タブ/ブラウザを閉じると消えます。ただし一部ブラウザの「前回のセッションを復元」機能を使うと残ってしまう場合があります(ブラウザ側の仕様で、こちらでは制御できません)。
- より厳重にしたい場合はリポジトリをprivateにする、またはGitHub Pagesではなく別の非公開ホスティングを使うことも検討してください。
- メッセージはGoogleスプレッドシートにそのまま保存されます。消したい場合はスプレッドシートの行を直接削除してください。
