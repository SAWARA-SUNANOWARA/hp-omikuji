# Hotline セットアップ手順

同じバンドを推している友達と2人だけでやり取りできる、通知なしの簡易連絡ページです。
既存のおみくじカウンター機能と同じ仕組み(GitHub Pages + Google Apps Script + Googleスプレッドシート)で動きます。無料で、あなた自身のGoogleアカウントの中だけでデータを持てます。

## 特徴

- LINEのように本名が出ない。好きなニックネームを名乗れる。
- プッシュ通知は一切送信しない。自分がページを開いたときだけ、その時点までのメッセージを取得する。
- Discord/SlackやXのDMとは完全に別の独立したページ。
- URLを開くと本物のおみくじ画面(`fortune.html`)がそのまま立ち上がり、普通に遊べる。画面を1.5秒以内に「右→左下→右」となぞる(Zの字を描く)と、隠しチャット画面`hotline-chat.html`に移動する。
- 実際の読み書きはサーバー側(Apps Script)で合言葉を照合するので、なぞり方やURLを知らない人はメッセージを見られない。

## 画面構成

- `fortune.html` … 表向きのダミー画面。中身は本物のおみくじ(`index.html`)と同一で、通常のタップ操作にはZジェスチャーの監視は一切干渉しない。
- `hotline-chat.html` … 実際のチャット画面。`fortune.html`からZジェスチャーで遷移してきたときだけ表示する想定(直接URLを知っていれば開けるが、どこからもリンクしていない)。
- 合言葉は`fortune.html`のURLに `?key=...` の形で付けておくと、`hotline-chat.html`まで自動的に引き継がれる。**画面上でパスワードを入力する操作は無い。**

## ここまで自動で完了していること

- 専用のGoogleスプレッドシートを作成済み:
  https://docs.google.com/spreadsheets/d/11qc3xptYhTIghD-wWEj8W2X5eJ0BzRbr4IvvZBuQymM/edit
- 合言葉をランダム生成済み(チャットの返信を参照)。
  **このリポジトリは公開(public)なので、合言葉の実際の値はコード内に書かず、Apps Scriptエディタ上でのみ設定してください。**

Google Apps Scriptの「デプロイ」操作はGoogleのセキュリティ上の理由でブラウザ上の手動クリックが必須のため、ここから先はあなた自身の操作が必要です。

## 残りの手順

### 1. Apps Scriptを開く

1. 上記スプレッドシートを開く。
2. メニューから「拡張機能」→「Apps Script」を開く。
3. 中身をすべて削除し、このリポジトリの `apps-script/hotline-backend.gs` の内容を貼り付ける。
4. コード内の `SECRET = "REPLACE_WITH_YOUR_OWN_SECRET"` を、チャットで渡された合言葉(またはあなたが決めた別の値)に書き換える。**このApps Scriptエディタ内での変更だけに留め、リポジトリには反映しない。**
5. 保存する(フロッピーアイコン、またはCtrl/Cmd+S)。

### 2. ウェブアプリとしてデプロイ

1. 右上の「デプロイ」→「新しいデプロイ」をクリック。
2. 種類の選択で「ウェブアプリ」を選ぶ。
3. 設定:
   - 実行するユーザー: **自分**
   - アクセスできるユーザー: **全員**
4. 「デプロイ」をクリックし、発行された **ウェブアプリのURL**(`https://script.google.com/macros/s/xxxx/exec` の形式)をコピーする。

> コードを更新したときは「新しいデプロイ」ではなく「デプロイを管理」→ 既存のデプロイを編集 → バージョンを新しくする、で更新できます。

### 3. フロントエンドに設定を反映

`hotline-data.js` を開き、以下を書き換える。

```js
const HOTLINE_API_URL = "YOUR_APPS_SCRIPT_EXEC_URL_HERE";
```

↓ 手順2でコピーしたURLに置き換える。

### 4. 公開・友達への共有

1. 変更をコミットしてpushすると、GitHub Pagesが有効なら
   `https://<ユーザー名>.github.io/hp-omikuji/fortune.html` でアクセスできるようになる。
2. 友達に伝えるURLは、合言葉付きの以下の形にする:
   `https://<ユーザー名>.github.io/hp-omikuji/fortune.html?key=合言葉`
   これをLINEなど**別の経路で一度だけ**こっそり伝える。ブックマークしてもらえば以後は毎回打つ必要はない。
3. 初回だけ「呼び名(ニックネーム)」を聞かれるので、それぞれ好きな名前を入力する。入力内容はその端末にだけ保存される。
4. 使い方: 通常は`fortune.html`を開いて普通におみくじを引ける。相手とやり取りしたい時だけ、画面を「右→左下→右」の順に1.5秒以内でなぞる(Zを描く)とチャット画面に切り替わる。

## 注意点

- `fortune.html` / `hotline-chat.html` はどちらもトップページ(`index.html`)からはリンクしていません。URLを知っている人しかたどり着けません。
- ただしリポジトリが公開(public)の場合、`fortune.html`のZジェスチャーの判定コードや`apps-script/hotline-backend.gs`のファイル自体は誰でも読めます。つまり「なぞり方」自体は隠しきれません。実際のセキュリティは、Apps Script側で照合する**合言葉(SECRET)**が担っています。**SECRETは必ず自分で書き換えたものを使い、貼り付けたままの値を使わないでください。**
- より厳重にしたい場合はリポジトリをprivateにする、またはGitHub Pagesではなく別の非公開ホスティングを使うことも検討してください。
- メッセージはGoogleスプレッドシートにそのまま保存されます。消したい場合はスプレッドシートの行を直接削除してください。
