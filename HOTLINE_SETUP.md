# Hotline セットアップ手順

同じバンドを推している友達と2人だけでやり取りできる、通知なしの簡易連絡ページです。
既存のおみくじカウンター機能と同じ仕組み(GitHub Pages + Google Apps Script + Googleスプレッドシート)で動きます。無料で、あなた自身のGoogleアカウントの中だけでデータを持てます。

## 特徴

- LINEのように本名が出ない。好きなニックネームを名乗れる。
- プッシュ通知は一切送信しない。自分がページを開いたときだけ、その時点までのメッセージを取得する。
- Discord/SlackやXのDMとは完全に別の独立したページ。
- 合言葉(自分たちだけの秘密の文字列)を知っている人しか読み書きできない。

## 手順

### 1. Googleスプレッドシートを作成

1. https://sheets.google.com で新しいスプレッドシートを作成する(名前は何でもよい)。

### 2. Apps Scriptを設定

1. スプレッドシートのメニューから「拡張機能」→「Apps Script」を開く。
2. 中身をすべて削除し、このリポジトリの `apps-script/hotline-backend.gs` の内容を貼り付ける。
3. コード内の `SECRET` を、自分たちだけの合言葉に書き換える。
   - 例: 適当な英数字を20文字くらいランダムに決める(バンド名や誕生日などの推測されやすい文字列は避ける)。
4. 保存する。

### 3. ウェブアプリとしてデプロイ

1. 右上の「デプロイ」→「新しいデプロイ」をクリック。
2. 種類の選択で「ウェブアプリ」を選ぶ。
3. 設定:
   - 実行するユーザー: **自分**
   - アクセスできるユーザー: **全員**
4. 「デプロイ」をクリックし、発行された **ウェブアプリのURL**(`https://script.google.com/macros/s/xxxx/exec` の形式)をコピーする。

> コードを更新したときは「新しいデプロイ」ではなく「デプロイを管理」→ 既存のデプロイを編集 → バージョンを新しくする、で更新できます。

### 4. フロントエンドに設定を反映

`hotline-data.js` を開き、以下を書き換える。

```js
const HOTLINE_API_URL = "YOUR_APPS_SCRIPT_EXEC_URL_HERE";
```

↓ 手順3でコピーしたURLに置き換える。

### 5. 公開・アクセス

1. 変更をコミットしてpushすると、GitHub Pagesが有効なら
   `https://<ユーザー名>.github.io/hp-omikuji/hotline.html` でアクセスできるようになる。
2. 初回アクセス時に、合言葉と呼び名(ニックネーム)を入力する。入力内容はその端末にだけ保存され、次回以降は自動で使われる。
3. 友達にもこのページのURLと合言葉を、LINEなど**別の経路で一度だけ**こっそり伝える。
   - URLに `?key=合言葉` を付けて共有すると、リンクを開くだけで合言葉入力が省略できる(例: `hotline.html?key=xxxxxxxx`)。

## 注意点

- `hotline.html` はトップページ(`index.html`)からはリンクしていません。URLを知っている人しかたどり着けません。
- ただしリポジトリが公開(public)の場合、`apps-script/hotline-backend.gs` のファイル自体は誰でも読めます。**SECRETは必ず自分で書き換えたものを使い、貼り付けたままの値を使わないでください。**
- より厳重にしたい場合はリポジトリをprivateにする、またはGitHub Pagesではなく別の非公開ホスティングを使うことも検討してください。
- メッセージはGoogleスプレッドシートにそのまま保存されます。消したい場合はスプレッドシートの行を直接削除してください。
